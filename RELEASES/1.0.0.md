# Release v1.0.0

Date: 2026-01-09

Published container images:

- ghcr.io/havartibard/mcp-proxmox:1.0.0
- ghcr.io/havartibard/mcp-proxmox:latest

Notes:

- Built from branch: feat/containerize
- Dockerfile located at repository root
- Build performed with `./build.sh` (which runs `docker build`).
- Images pushed to GHCR (ghcr.io) after authenticating with a PAT having `write:packages` scope.
